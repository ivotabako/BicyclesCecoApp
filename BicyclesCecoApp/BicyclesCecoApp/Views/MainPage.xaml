<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BicyclesCecoApp"
             x:Class="BicyclesCecoApp.MainPage"
             xmlns:customerVm="clr-namespace:BicyclesCecoApp.ViewModels">
    <ContentPage.BindingContext>
        <customerVm:EmployeeViewModel/>
    </ContentPage.BindingContext>
    <StackLayout Padding="20">
        <Button Text="Списък"
          Command="{Binding NavToListCommand}"/>
        <Button Text="Добави нов"
          Command="{Binding NavToCreateCommand}"/>
        
    </StackLayout>

</ContentPage>
